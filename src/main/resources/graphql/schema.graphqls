schema {
  query: Query
  mutation: Mutation
}

type User {
  id: ID!
  name: String!
}

type Query {
  users: [User]
}

type Mutation {
  createUser(id: ID!, name: String!): User
  updateUser(id: ID!, name: String): User
  deleteUser(id: ID!): User
} 